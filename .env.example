# Copy to .env and fill in. .env is gitignored and must stay that way.
# NEVER put real passwords in this file — it is the one .env* file that is
# NOT ignored, because it is meant to be committed as a template.
#
# Same email for both businesses, one password each (brief section 9).

AS_EMAIL=teamNN@theschoolofai.in

# Suryodaya — India, GST
AS_PASSWORD_SURYODAYA=

# Keystone — US, Sales & Use Tax
AS_PASSWORD_KEYSTONE=

# Model for the agent's "brain". Gemini's OpenAI-compatible endpoint.
SEAT15_LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
SEAT15_LLM_MODEL=
SEAT15_LLM_API_KEY=
